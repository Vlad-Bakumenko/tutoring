import { hoverHeader } from "./hover.js"; // import function
import { scrollWindow } from "./scroll.js"; // import function
import {clickImages} from "./click.js"; // import function

hoverHeader(); // calling imported function, so it will apply it to html file 
scrollWindow(); // calling imported function, so it will apply it to html file 
clickImages(); // calling imported function, so it will apply it to html file